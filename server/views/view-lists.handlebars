<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Caspartool {{SPX.title}} | {{lang 'pagetitle.select'}}</title>
  <link rel="stylesheet" type="text/css" href="/css/spx_layout.css">
  <link rel="stylesheet" type="text/css" href="/css/spx_forms.css">
  <link rel="stylesheet" type="text/css" href="/css/spx_colors.css">
  <script src="/js/socket.io.js"></script>
  <script src="/js/spx_caspartool.js"></script>
</head>

<body class="bg_black">
  <div class="spxMainPanel bg_grey60">
    <div class="spxTopRow darkBg">
      <div class="spxCell">
        {{>navi}}
      </div>
    </div>
    <div class="spxMainSection">
      <table style="width:55%; height:75%; margin:auto;">
        <tr>
          <td style="height: 2em; padding:0.7em; color:white;" class="darkBg" align="left">
            <span class="spxTableHead">{{lang 'headline.select'}}</span><BR>
            <span class="spxTableHeadsmall">{{lang 'general.folder'}}: {{folder}}</span>
            <BR><span class="spxTableHeadsmall" style="background-color: rgb(248, 0, 0);">{{errorMsg}}</span>
          </td>
        </tr>
        <tr>
          <td>
            <div style="width:100%; height:100%; overflow:hidden; color:white;">
              <select id="lists" size=10 class="spxformtext bg_grey40"
                style="width: 100%; height:100%; color:white; border:0px; padding:0.2em; line-height:3;"
                ondblclick="edi();" autofocus>
                {{#each files}}
                <option>{{name}}</option>
                {{/each}}
              </select><BR>
            </div>
          </td>
        </tr>
        <tr>
          <td class="spxFlexContainer">
            <button class="spxformtext btn spxFlexItem bg_blue ripple"
              onClick="add();">{{lang 'button.add'}}...</button>
            <button class="spxformtext btn spxFlexItem bg_blue ripple"
              onClick="del();">{{lang 'button.delete'}}</button>
            <button class="spxformtext btn spxFlexItem bg_blue ripple" onClick="edi();">{{lang 'button.edit'}}</button>
            <button class="spxformtext btn spxFlexItem bg_orange ripple"
              onClick="cas();">{{lang 'button.open'}}</button>
          </td>
        </tr>
      </table>
    </div>

    <script>
      window.onload = function () {
        document.getElementById('lists').selectedIndex = "0";
      }
    </script>

  </div>
</body>

</html>